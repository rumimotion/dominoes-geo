mkdir -p ~/Documents/DOMINO/dominoes/src/app/solo
git add .
git commit -m "feat: solo test mode"
git push
cd ~/Documents/DOMINO/dominoes
mkdir -p src/app/solo
cd ~/Documents/DOMINO/dominoes
git add .
git commit -m "feat: solo test mode"
